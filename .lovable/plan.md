# OxynHost Landing Page

## Build
- Replace the placeholder with a responsive dark OxynHost landing page using the supplied cyan/blue visual direction.
- Add the full navigation, split hero, domain search, terminal preview, trust metrics, feature grid, pricing plans, CTA, and footer.
- Add smooth restrained motion, glass surfaces, glow borders, mobile navigation, and accessible keyboard/focus states.

## Interactions
- Make domain availability checks validate input and show a result toast.
- Add monthly/yearly pricing with a 20% yearly discount and clearly updated prices.
- Add an order modal with plan selection, validated name/contact fields, a fresh math challenge, error feedback, and success state.

## Secure order delivery
- Connect the project to the Telegram Bot API.
- Send validated order details through a server-only function so credentials never reach the browser.
- Add safe server-side validation and clear provider errors; no customer details will be persisted.

## Quality checks
- Add page-specific social and search metadata.
- Verify the production build, desktop and mobile layouts, domain search, pricing switch, modal validation, and order flow.
